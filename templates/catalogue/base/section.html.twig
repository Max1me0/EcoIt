<li class="mb-1">
    <div >
    {{section.title}}
    </div>
    <div>
        <ul class="nav nav-pills flex-column mb-auto">
        {% for lesson in section.lessons %}
            <li class="nav-item">
                <a href="{{ path('catalogue_formation_consulter', {'formation': formation.id, 'section': section.id, 'lesson': lesson.id}) }}" class="nav-link">{{lesson.title}}</a>
            </li>
        {% endfor %}
        </ul>
    </div>
</li>