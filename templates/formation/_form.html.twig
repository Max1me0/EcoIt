{{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
    {% for section in formation.sections %}
    <p>{{ section.title }}
    {% endfor %}
    </div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
